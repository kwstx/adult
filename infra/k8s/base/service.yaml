apiVersion: v1
kind: Service
metadata:
  name: auralive-web-service
  labels:
    app.kubernetes.io/name: auralive-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: auralive-web
